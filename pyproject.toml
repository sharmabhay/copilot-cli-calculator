[project]
name = "calculator"
version = "0.1.0"
description = "A Python calculator with CLI and Streamlit web UI"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.56.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.9",
]
